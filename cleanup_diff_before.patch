diff --git a/src/commands/audit.js b/src/commands/audit.js
index 1d410db..43e818c 100644
--- a/src/commands/audit.js
+++ b/src/commands/audit.js
@@ -1,6 +1,8 @@
 import fs from 'node:fs';
 import path from 'node:path';
 import { scanNamingIssues } from '../utils/naming.js';
+import { auditQualityGates } from '../utils/quality-gates.js';
+import { getMarkdownSection, hasFilledListItem } from '../utils/markdown-checks.js';
 
 const MAIN_SUBFOLDERS = [
   '00_ddad',
@@ -32,16 +34,6 @@ const SESSION_SUBFOLDERS = [
   '13_release',
 ];
 
-const QUALITY_GATES = [
-  'architecture_gate.md',
-  'security_gate.md',
-  'tests_gate.md',
-  'performance_gate.md',
-  'design_gate.md',
-  'deploy_gate.md',
-  'final_audit_gate.md',
-];
-
 const ROOT_FILES = ['CLAUDE.md', 'AGENTS.md', '.cursorrules'];
 
 function mdFiles(dirPath) {
@@ -51,7 +43,17 @@ function mdFiles(dirPath) {
   return fs.readdirSync(dirPath).filter((name) => name.endsWith('.md') && name !== 'README.md');
 }
 
-function auditSessions(sessionsDir, warnings) {
+function auditPendencies(feedbackPath, label, errors, warnings) {
+  const content = fs.readFileSync(feedbackPath, 'utf8');
+  if (hasFilledListItem(getMarkdownSection(content, 'P1', '###'))) {
+    errors.push(`Pendência P1 (crítica) aberta em: ${label}`);
+  }
+  if (hasFilledListItem(getMarkdownSection(content, 'P2', '###'))) {
+    warnings.push(`Pendência P2 (importante) aberta em: ${label}`);
+  }
+}
+
+function auditSessions(sessionsDir, errors, warnings) {
   if (!fs.existsSync(sessionsDir)) {
     return;
   }
@@ -75,8 +77,9 @@ function auditSessions(sessionsDir, warnings) {
       .map((name) => name.replace(/\.md$/, ''));
     const prompts = mdFiles(path.join(sessionDir, '06_prompts'))
       .map((name) => name.replace(/^prompt_/, '').replace(/\.md$/, ''));
-    const feedbacks = mdFiles(path.join(sessionDir, '08_feedbacks'))
-      .map((name) => name.replace(/^feedback_/, '').replace(/\.md$/, ''));
+    const feedbackFiles = mdFiles(path.join(sessionDir, '08_feedbacks'));
+    const feedbacks = feedbackFiles.map((name) => name.replace(/^feedback_/, '').replace(/\.md$/, ''));
+    const validations = mdFiles(path.join(sessionDir, '09_validation'));
 
     for (const block of blocks) {
       if (!prompts.includes(block)) {
@@ -96,6 +99,13 @@ function auditSessions(sessionsDir, warnings) {
         warnings.push(`Feedback órfão (sem bloco correspondente): Docs/05_sessions/${sessionName}/08_feedbacks/feedback_${feedback}.md`);
       }
     }
+    for (const feedbackFile of feedbackFiles) {
+      const label = `Docs/05_sessions/${sessionName}/08_feedbacks/${feedbackFile}`;
+      auditPendencies(path.join(sessionDir, '08_feedbacks', feedbackFile), label, errors, warnings);
+    }
+    if (fs.existsSync(path.join(sessionDir, '09_validation')) && validations.length === 0) {
+      warnings.push(`Sessão sem validação preenchida: Docs/05_sessions/${sessionName}/09_validation`);
+    }
   }
 }
 
@@ -123,10 +133,12 @@ export async function auditCommand({ dir }) {
     }
   }
 
-  for (const gate of QUALITY_GATES) {
-    if (!fs.existsSync(path.join(docsDir, '06_quality_gates', gate))) {
-      warnings.push(`Quality gate ausente: Docs/06_quality_gates/${gate}`);
-    }
+  const gatesAudit = auditQualityGates(path.join(docsDir, '06_quality_gates'));
+  errors.push(...gatesAudit.errors);
+  warnings.push(...gatesAudit.warnings);
+
+  if (!fs.existsSync(path.join(docsDir, '00_ddad', 'metodologia.md'))) {
+    warnings.push('Arquivo ausente: Docs/00_ddad/metodologia.md');
   }
 
   for (const file of ROOT_FILES) {
@@ -135,10 +147,11 @@ export async function auditCommand({ dir }) {
     }
   }
 
-  auditSessions(path.join(docsDir, '05_sessions'), warnings);
+  auditSessions(path.join(docsDir, '05_sessions'), errors, warnings);
 
   const naming = scanNamingIssues(docsDir);
   errors.push(...naming.errors);
+  warnings.push(...naming.warnings);
 
   const status = errors.length === 0 ? 'OK' : 'FAILED';
 
diff --git a/src/commands/validate.js b/src/commands/validate.js
index f1f0985..6f8a4ad 100644
--- a/src/commands/validate.js
+++ b/src/commands/validate.js
@@ -1,6 +1,7 @@
 import fs from 'node:fs';
 import path from 'node:path';
 import { scanNamingIssues } from '../utils/naming.js';
+import { getRequiredQualityGates, validateQualityGateContent } from '../utils/quality-gates.js';
 
 const MAIN_SUBFOLDERS = [
   '00_ddad',
@@ -36,6 +37,17 @@ export async function validateCommand({ dir }) {
       errors.push('Arquivo ausente: Docs/00_ddad/metodologia.md');
     }
 
+    const gatesDir = path.join(docsDir, '06_quality_gates');
+    for (const gate of getRequiredQualityGates()) {
+      const gatePath = path.join(gatesDir, gate);
+      const label = `Docs/06_quality_gates/${gate}`;
+      if (!fs.existsSync(gatePath)) {
+        errors.push(`Quality gate obrigatório ausente: ${label}`);
+        continue;
+      }
+      errors.push(...validateQualityGateContent(fs.readFileSync(gatePath, 'utf8'), label));
+    }
+
     const sessionsDir = path.join(docsDir, '05_sessions');
     if (fs.existsSync(sessionsDir)) {
       const sessionNumbers = fs.readdirSync(sessionsDir)
diff --git a/src/templates/quality_gates/architecture_gate.md b/src/templates/quality_gates/architecture_gate.md
index 983d9eb..2c64c71 100644
--- a/src/templates/quality_gates/architecture_gate.md
+++ b/src/templates/quality_gates/architecture_gate.md
@@ -55,3 +55,19 @@ Garantir que mudanças não comprometam a consistência arquitetural, a separaç
 - [ ] Aprovado com ressalvas
 - [ ] Reprovado
 - [ ] Bloqueado
+
+## 10. Responsável pela Validação
+
+_Quem executou esta validação — pessoa humana ou agente de IA, e sob supervisão de quem._
+
+_..._
+
+## 11. Data da Validação
+
+_..._
+
+## 12. Observações
+
+_Contexto adicional relevante para quem ler este gate depois: exceções aceitas, ressalvas, links para evidências externas. Se nenhuma, escreva "Nenhuma observação adicional"._
+
+_..._
diff --git a/src/templates/quality_gates/deploy_gate.md b/src/templates/quality_gates/deploy_gate.md
index fb90733..23e83c9 100644
--- a/src/templates/quality_gates/deploy_gate.md
+++ b/src/templates/quality_gates/deploy_gate.md
@@ -52,3 +52,19 @@ Garantir que toda promoção para homologação ou produção seja segura, verif
 - [ ] Aprovado com ressalvas
 - [ ] Reprovado
 - [ ] Bloqueado
+
+## 10. Responsável pela Validação
+
+_Quem executou esta validação — pessoa humana ou agente de IA, e sob supervisão de quem._
+
+_..._
+
+## 11. Data da Validação
+
+_..._
+
+## 12. Observações
+
+_Contexto adicional relevante para quem ler este gate depois: exceções aceitas, ressalvas, links para evidências externas. Se nenhuma, escreva "Nenhuma observação adicional"._
+
+_..._
diff --git a/src/templates/quality_gates/design_gate.md b/src/templates/quality_gates/design_gate.md
index 86cab94..74af905 100644
--- a/src/templates/quality_gates/design_gate.md
+++ b/src/templates/quality_gates/design_gate.md
@@ -51,3 +51,19 @@ Garantir que mudanças de UI respeitem a identidade visual e o design system, e
 - [ ] Aprovado com ressalvas
 - [ ] Reprovado
 - [ ] Bloqueado
+
+## 10. Responsável pela Validação
+
+_Quem executou esta validação — pessoa humana ou agente de IA, e sob supervisão de quem._
+
+_..._
+
+## 11. Data da Validação
+
+_..._
+
+## 12. Observações
+
+_Contexto adicional relevante para quem ler este gate depois: exceções aceitas, ressalvas, links para evidências externas. Se nenhuma, escreva "Nenhuma observação adicional"._
+
+_..._
diff --git a/src/templates/quality_gates/final_audit_gate.md b/src/templates/quality_gates/final_audit_gate.md
index 1935bc1..0b55fe5 100644
--- a/src/templates/quality_gates/final_audit_gate.md
+++ b/src/templates/quality_gates/final_audit_gate.md
@@ -54,3 +54,19 @@ Verificar, ao final de uma sessão (ou do projeto), que toda a metodologia DDAD
 - [ ] Aprovado com ressalvas
 - [ ] Reprovado
 - [ ] Bloqueado
+
+## 10. Responsável pela Validação
+
+_Quem executou esta validação — pessoa humana ou agente de IA, e sob supervisão de quem._
+
+_..._
+
+## 11. Data da Validação
+
+_..._
+
+## 12. Observações
+
+_Contexto adicional relevante para quem ler este gate depois: exceções aceitas, ressalvas, links para evidências externas. Se nenhuma, escreva "Nenhuma observação adicional"._
+
+_..._
diff --git a/src/templates/quality_gates/performance_gate.md b/src/templates/quality_gates/performance_gate.md
index 33b6e49..3abfa57 100644
--- a/src/templates/quality_gates/performance_gate.md
+++ b/src/templates/quality_gates/performance_gate.md
@@ -53,3 +53,19 @@ Garantir que a mudança não degrade tempo de resposta, uso de recursos ou exper
 - [ ] Aprovado com ressalvas
 - [ ] Reprovado
 - [ ] Bloqueado
+
+## 10. Responsável pela Validação
+
+_Quem executou esta validação — pessoa humana ou agente de IA, e sob supervisão de quem._
+
+_..._
+
+## 11. Data da Validação
+
+_..._
+
+## 12. Observações
+
+_Contexto adicional relevante para quem ler este gate depois: exceções aceitas, ressalvas, links para evidências externas. Se nenhuma, escreva "Nenhuma observação adicional"._
+
+_..._
diff --git a/src/templates/quality_gates/security_gate.md b/src/templates/quality_gates/security_gate.md
index 210c647..da64266 100644
--- a/src/templates/quality_gates/security_gate.md
+++ b/src/templates/quality_gates/security_gate.md
@@ -57,3 +57,19 @@ Garantir que nenhuma mudança introduza vulnerabilidade, exposição de dados se
 - [ ] Aprovado com ressalvas
 - [ ] Reprovado
 - [ ] Bloqueado
+
+## 10. Responsável pela Validação
+
+_Quem executou esta validação — pessoa humana ou agente de IA, e sob supervisão de quem._
+
+_..._
+
+## 11. Data da Validação
+
+_..._
+
+## 12. Observações
+
+_Contexto adicional relevante para quem ler este gate depois: exceções aceitas, ressalvas, links para evidências externas. Se nenhuma, escreva "Nenhuma observação adicional"._
+
+_..._
diff --git a/src/templates/quality_gates/tests_gate.md b/src/templates/quality_gates/tests_gate.md
index f2221ed..37bcf42 100644
--- a/src/templates/quality_gates/tests_gate.md
+++ b/src/templates/quality_gates/tests_gate.md
@@ -51,3 +51,19 @@ Garantir que o comportamento esperado de um bloco está verificado, não apenas
 - [ ] Aprovado com ressalvas
 - [ ] Reprovado
 - [ ] Bloqueado
+
+## 10. Responsável pela Validação
+
+_Quem executou esta validação — pessoa humana ou agente de IA, e sob supervisão de quem._
+
+_..._
+
+## 11. Data da Validação
+
+_..._
+
+## 12. Observações
+
+_Contexto adicional relevante para quem ler este gate depois: exceções aceitas, ressalvas, links para evidências externas. Se nenhuma, escreva "Nenhuma observação adicional"._
+
+_..._
